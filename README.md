# lan-share

Share files via LAN.

## Usage

```bash
$ npm install -g lan-share
$ lan-share [ <path> [ <port> ] ]
```

- `<path>`: Path for sharing (default: current path)
- `<port>`: HTTP port (default: `9527`)
